# JLO (Just Logs) Environment Configuration
# Copy this file to .env and set the required values

# ============================================================================
# REQUIRED: JWT Secret Key
# ============================================================================
# Used to sign authentication tokens (JWTs)
# NEVER commit this to version control!
# 
# Generate a secure secret key with:
#   openssl rand -hex 32
#
JLO_SECRET_KEY=CHANGE_ME_GENERATE_WITH_OPENSSL_RAND_HEX_32

# ============================================================================
# REQUIRED: Admin Password (First-time Setup Only)
# ============================================================================
# Password for the default admin user (username: admin)
# Only used when creating the initial admin account
# After first login, change this password via the web UI
#
# Use a strong password with:
#   - At least 12 characters
#   - Mix of uppercase, lowercase, numbers, and symbols
#
JLO_ADMIN_PASSWORD=CHANGE_ME_USE_STRONG_PASSWORD

# ============================================================================
# OPTIONAL: Environment Mode
# ============================================================================
# Set to "development" for local development (allows HTTP cookies)
# Set to "production" for production deployment (enforces HTTPS cookies)
# Default: production
#
JLO_ENV=production

# ============================================================================
# OPTIONAL: CORS Origins
# ============================================================================
# Comma-separated list of allowed origins for web UI access
# Required when deploying to production domains
# Always includes localhost:5173, localhost:8000, 127.0.0.1:5173, 127.0.0.1:8000
#
# Examples:
#   Single domain:  JLO_CORS_ORIGINS=https://logs.example.com
#   Multiple:       JLO_CORS_ORIGINS=https://logs.example.com,https://logs.backup.com
#
# JLO_CORS_ORIGINS=https://logs.lets-work.ai

# ============================================================================
# OPTIONAL: Server Configuration
# ============================================================================
# Uncomment and modify as needed
#
# JLO_HOST=0.0.0.0
# JLO_PORT=8000
# JLO_WORKERS=4

# ============================================================================
# OPTIONAL: Database Configuration
# ============================================================================
# Path to SQLite database file
# Default: jlo.db (in backend directory)
#
# JLO_DB_PATH=/app/backend/data/jlo.db
